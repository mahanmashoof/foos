export enum Paths {
    HOME = '/',
    RULES = '/rules',
    ADMIN = '/admin',
    PASSWORD = '/password',
    TEAMS = '/teams',
    RESULTS = '/results'
}

export enum GameStatus {
    NOT_PLAYED = 0,
    FINISHED = 1,
}

export enum GameResult {
    WIN = 3,
    CLOSE_LOSS = 1,
    DEFEAT = 0,
}