export enum Paths {
    HOME = '/',
    RULES = '/rules',
    ADMIN = '/admin',
    PASSWORD = '/password',
    TEAMS = '/teams'
}

export enum GameStatus {
    NOT_PLAYED = 0,
    FINISHED = 1,
}

export enum GameResult {
    WIN = 3,
    DEFEAT = 0,
}